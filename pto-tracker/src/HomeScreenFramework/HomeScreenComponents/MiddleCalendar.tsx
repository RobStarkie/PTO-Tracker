import React from "react";
import './MiddleCalendar.css'

interface MiddleCalendarProps {
    content: string;
}

const MiddleCalendar: React.FC<MiddleCalendarProps> = ({ content }) => {
    return (
        <div>

        </div>
    );
};

export default MiddleCalendar;